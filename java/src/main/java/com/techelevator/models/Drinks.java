package com.techelevator.models;

import java.math.BigDecimal;

public class Drinks extends Items{
    public Drinks(String slot, String name, BigDecimal price, String type) {
        super(slot, name, price, type);
    }
}
